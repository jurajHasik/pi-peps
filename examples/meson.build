
examples = [
            'opt-fu-adaptive',
            'opt-su-adaptive',
            'env-ctmrg'
]

foreach _example : examples
    subdir(_example) # build the example
    # TODO: install the example with proper meson.build
endforeach
